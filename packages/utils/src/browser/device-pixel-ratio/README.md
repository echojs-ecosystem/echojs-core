# device-pixel-ratio

Signal-native composable — see [ReactUse](https://reactuse.org/new/docs/functions) for inspiration.

> Not a React hook. Call `dispose()` when done.

```ts
import { devicePixelRatio } from "@echojs-ecosystem/utils/device-pixel-ratio";
```

SSR-safe. See `device-pixel-ratio.ts` for API.
