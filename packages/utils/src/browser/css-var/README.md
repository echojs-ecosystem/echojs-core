# css-var

Signal-native composable — see [ReactUse](https://reactuse.org/new/docs/functions) for inspiration.

> Not a React hook. Call `dispose()` when done.

```ts
import { cssVar } from "@echojs-ecosystem/utils/css-var";
```

SSR-safe. See `css-var.ts` for API.
