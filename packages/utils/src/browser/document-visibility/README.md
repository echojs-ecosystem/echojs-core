# document-visibility

Signal-native composable — see [ReactUse](https://reactuse.org/new/docs/functions) for inspiration.

> Not a React hook. Call `dispose()` when done.

```ts
import { documentVisibility } from "@echojs-ecosystem/utils/document-visibility";
```

SSR-safe. See `document-visibility.ts` for API.
