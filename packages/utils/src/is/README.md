# is — type guards

TypeScript-first type guards and emptiness helpers.

```ts
import { isString, isEmptyArray, isNullable } from "@echojs-ecosystem/utils/is";
```

See [`is.ts`](./is.ts) for the full API.
