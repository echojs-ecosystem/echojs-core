---
title: withStorage
description: withStorage — Extensions API.
package: '@echojs-ecosystem/persist'
keywords: [withStorage, persist]
---

@echojs-ecosystem/persist

## Usage

```ts
import { persist, withLocalStorage } from '@echojs-ecosystem/persist'

persist(fieldLike, withLocalStorage({ key: 'profile:name' }))
```

## Type Declarations

```ts
import { persist, withLocalStorage } from '@echojs-ecosystem/persist'

persist(fieldLike, withLocalStorage({ key: 'profile:name' }))
```

## API

### Returns

`withStorage` — see Type Declarations for the full signature.
