---
import DocsLayout from "../../../../layouts/DocsLayout.astro";
import { packageNav } from "../../../../lib/docs-nav";

const nav = packageNav.reactivity;
---

<DocsLayout title="computed" description="API: computed(() => value)" nav={nav}>
  <h1 class="docH1"><span class="mono">computed(() =&gt; value)</span></h1>
  <p class="docLead">
    Создаёт readonly computed signal. Ленивый пересчёт, инвалидируется при изменениях зависимостей.
  </p>

  <hr class="docHr" />

  <div class="callout">Дальше: контракты, ошибки и примеры.</div>
</DocsLayout>

